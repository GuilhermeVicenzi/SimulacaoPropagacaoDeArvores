<?xml version='1.0' encoding='utf-8'?>

<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Banco -->
        <property name="hibernate.connection.driver_class">
            org.sqlite.JDBC
        </property>

        <property name="hibernate.connection.url">
            jdbc:sqlite:simulacoes.db
        </property>

        <!-- Dialect -->
        <property name="hibernate.dialect">org.hibernate.community.dialect.SQLiteDialect</property>

        <!-- Debug -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Cria/atualiza tabelas automaticamente -->
        <property name="hibernate.hbm2ddl.auto">
            update
        </property>

        <!-- Entidades -->
        <mapping class="simulacao.simulador.Simulador"/>
        <mapping class="simulacao.simulador.Simulacao"/>
        <mapping class="simulacao.simulador.EstatisticasIteracao"/>

    </session-factory>
</hibernate-configuration>